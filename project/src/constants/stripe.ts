// Stripe price IDs — loaded from environment variables for easy switching
export const PRICE_MONTHLY = import.meta.env.VITE_STRIPE_PRICE_MONTHLY;
export const PRICE_YEARLY  = import.meta.env.VITE_STRIPE_PRICE_YEARLY;
